asterics.utils = {
    createCellBoardItem: function(title, imgUrl, clickAction) {
        return {
            title: title,
            faIcon: imgUrl,
            clickAction: clickAction
        };
    }
};
